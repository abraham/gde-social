{
  "name": "root",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap --no-ci",
    "build": "lerna run build",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "3.11.0"
  }
}

{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "fix": "prettier --write .",
    "lint": "prettier --check . && lerna run lint",
    "postinstall": "lerna bootstrap --no-ci",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "3.22.1",
    "prettier": "2.2.0"
  }
}

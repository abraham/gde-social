{
  "name": "gde-social",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "fix": "prettier --write .",
    "lint": "prettier --check . && lerna run lint",
    "postinstall": "lerna bootstrap --no-ci",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "5.1.6",
    "prettier": "2.6.2"
  },
  "engines": {
    "node": "16",
    "npm": "7"
  }
}

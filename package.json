{
  "name": "gde-social",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "fix": "prettier --write .",
    "lint": "prettier --check . && lerna run lint",
    "postinstall": "lerna bootstrap --no-ci",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "6.4.1",
    "prettier": "2.8.3"
  },
  "engines": {
    "node": "16",
    "npm": "7"
  }
}
